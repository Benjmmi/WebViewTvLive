<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2025 AITVLive
  ~
  ~ 文件: item_settings.xml
  ~ 描述: 设置项列表项布局
  ~
  ~ 布局结构：水平LinearLayout，从左到右：
  ~ 1. 设置项标题（TextView，占据剩余空间）
  ~ 2. 选项调整容器（LinearLayout，固定宽度200dp）
  ~    - 向左按钮（Button）
  ~    - 当前选项（TextView）
  ~    - 向右按钮（Button）
  ~
  ~ 设置项类型：
  ~ - 可选择型：显示标题 + 调整按钮 + 当前值
  ~ - 点击型：只显示标题（隐藏选项调整容器）
  ~
  ~ 使用场景：
  ~ - 在AppSettingsView的RecyclerView中使用
  ~ - 显示应用级别的设置项
  ~
  ~ 对应的代码文件：
  ~ - AppSettingsView.kt (应用设置视图控制器)
  ~ - AppSettingsView.ViewHolder (视图持有者)
  -->

<!--
  ~ 根容器：水平LinearLayout
  ~
  ~ 高度：35dp（固定高度）
  ~ 背景：@drawable/bg_item（带焦点状态）
  ~ 焦点：可获得焦点（focusable="true"）
  ~ 方向：horizontal（水平排列）
  ~ 内边距：左右各10dp
  ~
  ~ 子视图布局：
  ~ - 左侧：设置项标题（weight=1，占据剩余空间）
  ~ - 右侧：选项调整容器（固定宽度200dp）
  -->
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent"
	android:layout_height="35dp"
	android:background="@drawable/bg_item"
	android:focusable="true"
	android:orientation="horizontal"
	android:paddingLeft="10dp"
	android:paddingRight="10dp">

	<!--
	  ~ 设置项标题
	  ~
	  ~ 位置：左侧
	  ~ 宽度：0dp + weight=1（占据剩余空间）
	  ~ 高度：match_parent（填充父容器高度）
	  ~ 对齐：center_vertical（垂直居中）
	  ~ 字体大小：17sp
	  ~
	  ~ 显示内容：设置项的名称
	  ~ - 例如："频道列表"、"最大加载时间"、"刷新频道列表"
	  ~
	  ~ 对应代码：AppSettingsView.kt
	  ~ - ViewHolder.bind() 方法设置标题
	  ~ - 数据源：SettingItem.titleRes（字符串资源ID）
	  -->
	<TextView
		android:id="@+id/tvTitle"
		android:layout_width="0dp"
		android:layout_height="match_parent"
		android:layout_weight="1"
		android:gravity="center_vertical"
		android:textSize="17sp" />

	<!--
	  ~ 选项调整容器
	  ~
	  ~ 位置：右侧
	  ~ 宽度：200dp（固定宽度）
	  ~ 高度：match_parent（填充父容器高度）
	  ~ 方向：horizontal（水平排列）
	  ~
	  ~ 可见性：
	  ~ - 可选择型设置项：VISIBLE
	  ~ - 点击型设置项：GONE
	  ~
	  ~ 子视图（从左到右）：
	  ~ - 向左按钮（宽度50dp）
	  ~ - 当前选项（weight=1，占据剩余空间）
	  ~ - 向右按钮（宽度50dp）
	  ~
	  ~ 对应代码：AppSettingsView.kt
	  ~ - ViewHolder.bind() 控制显示/隐藏
	  ~ - 根据SettingItem.items是否为空决定
	  -->
	<LinearLayout
		android:id="@+id/llItem"
		android:layout_width="200dp"
		android:layout_height="match_parent"
		android:orientation="horizontal">

		<!--
		  ~ 向左调整按钮
		  ~
		  ~ 宽度：50dp（固定宽度）
		  ~ 高度：match_parent（填充父容器高度）
		  ~ 背景：@drawable/bg_item（带焦点状态）
		  ~ 对齐：center（居中）
		  ~ 文本："◀"（向左箭头）
		  ~
		  ~ 功能：
		  ~ - 点击切换到上一个选项（循环）
		  ~ - 配合遥控器左键使用
		  ~
		  ~ 对应代码：AppSettingsView.kt
		  ~ - ViewHolder.adjustItem(false) 方法
		  ~ - 使用adjustValue()实现循环
		  -->
		<Button
			android:id="@+id/btnLeft"
			android:layout_width="50dp"
			android:layout_height="match_parent"
			android:background="@drawable/bg_item"
			android:gravity="center"
			android:text="◀" />

		<!--
		  ~ 当前选项显示
		  ~
		  ~ 位置：中间
		  ~ 宽度：0dp + weight=1（占据剩余空间）
		  ~ 高度：match_parent（填充父容器高度）
		  ~ 对齐：center（居中）
		  ~ 字体大小：17sp
		  ~
		  ~ 显示内容：当前选中的选项文本
		  ~ - 例如："default"（频道列表名）
		  ~ - 例如："15秒"（最大加载时间）
		  ~ - 例如："开启"/"关闭"（WebView可触摸）
		  ~
		  ~ 对应代码：AppSettingsView.kt
		  ~ - ViewHolder.bind() 方法初始化显示
		  ~ - ViewHolder.adjustItem() 方法更新显示
		  ~ - 数据源：SettingItem.items[selectedItemPosition]
		  -->
		<TextView
			android:id="@+id/tvItem"
			android:layout_width="0dp"
			android:layout_height="match_parent"
			android:layout_weight="1"
			android:gravity="center"
			android:textSize="17sp" />

		<!--
		  ~ 向右调整按钮
		  ~
		  ~ 宽度：50dp（固定宽度）
		  ~ 高度：match_parent（填充父容器高度）
		  ~ 背景：@drawable/bg_item（带焦点状态）
		  ~ 对齐：center（居中）
		  ~ 文本："▶"（向右箭头）
		  ~
		  ~ 功能：
		  ~ - 点击切换到下一个选项（循环）
		  ~ - 配合遥控器右键使用
		  ~
		  ~ 对应代码：AppSettingsView.kt
		  ~ - ViewHolder.adjustItem(true) 方法
		  ~ - 使用adjustValue()实现循环
		  ~
		  ~ 完整显示效果（可选择型设置项）：
		  ~ ┌─────────────────────────────────────────┐
		  ~ │ 频道列表        ◀  default  ▶         │
		  ~ │ 最大加载时间    ◀  15秒     ▶         │
		  ~ │ WebView可触摸   ◀  开启     ▶         │
		  ~ └─────────────────────────────────────────┘
		  ~
		  ~ 完整显示效果（点击型设置项）：
		  ~ ┌─────────────────────────────────────────┐
		  ~ │ 刷新频道列表                            │
		  ~ └─────────────────────────────────────────┘
		  -->
		<Button
			android:id="@+id/btnRight"
			android:layout_width="50dp"
			android:layout_height="match_parent"
			android:background="@drawable/bg_item"
			android:gravity="center"
			android:text="▶" />
	</LinearLayout>

</LinearLayout>
